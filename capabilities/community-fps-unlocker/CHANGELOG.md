# Changelog — community-fps-unlocker

## 1.0.0 — 2026-09-21

- Initial capability recipe mirroring the upstream FPS Unlocker flow.
- Example-only capability demonstrating the kill-process + extract-zip
  + move-file chain the runner consumes.
- Wraps the existing `dxgi.dll` proxy the upstream tool ships.

---

Maintainer: petonexus community.
Maintainer signing key: TBD (first release pins it via
`public-keys.json`).